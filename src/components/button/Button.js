import React from 'react';
import './Button.css';

const Button = () => {
    return (
        <div>
            <a href="#view_more" className="button">Apply</a>
        </div>
    );
};

export default Button;